{
  "id": "csv-uploader",
  "name": "CSV Uploader",
  "logo": "/img/csv-uploader-logo.png",
  "description": "Plugin to allow you to upload entire CSV files to Ceramic.",
  "hooks": ["generate"],
  "routes": ["upload"],
  "variables": [
    {
      "name": "Model ID",
      "description": "The model ID used by the streams created by the plugin. It must fit the columns names of your CSV file.",
      "id": "model_id"
    }
  ]
}
